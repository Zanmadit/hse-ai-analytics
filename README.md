# AI-Аналитика HSE-системы

> [English version of the documentation is available here](./README_EN.md)

Интеллектуальный модуль прогнозной аналитики для систем охраны труда и промышленной безопасности.

Данное решение объединяет данные о происшествиях и результатах проверок соблюдения правил безопасности, превращая историческую статистику в инструмент для активного предотвращения травм.

[Задание](https://astanahub.com/ru/tech_task/vnedrenie-ii-v-sfere-okhrany-truda1772626245)

## Основные функции:
- **Прогностическая аналитика**: прогнозирование количества инцидентов на 3, 6 и 12 месяцев.
- **Оценка рисков**: автоматическое выявление «5 основных зон риска» среди организаций и производственных объектов.
- **Интерактивный AI Чат с Базой Данных**: возможность задавать вопросы на естественном языке благодаря интеграции с локальной LLM-моделью Ollama (LLaMA3), которая автономно генерирует и выполняет SQL-запросы к PostgreSQL.
- **Система оповещения**: автоматические уведомления при обнаружении систематических нарушений в модуле «Карты Коргау».
- **Экономический эффект**: динамический расчет рентабельности инвестиций (ROI) и сорванных затрат (в тенге) на основе снижения показателей травматизма.
- **Рекомендации**: формирование конкретных профилактических мер на основе анализа данных.

## Структура проекта
```
.
├── app
│   ├── dashboard.py               # Основной Frontend (Streamlit)
│   ├── llm                        
│   │   └── db_chat.py             # Агент LangChain для перевода текста в SQL
│   ├── ml
│   │   ├── forecast.py            # Модели прогнозирования
│   │   └── risk_score.py          # Логика расчета риск-скоринга
│   └── utils
│       ├── data_loader.py         # Подключение и парсинг БД
│       ├── db.py                  # Настройка SQLAlchemy базы данных
│       ├── init_db.py             # Скрипт первоначальной загрузки CSV в БД
│       └── models.py              # ORM схемы PostgreSQL таблиц
├── data                           # Исходные файлы датасетов
│   ├── incidents_upd.csv          
│   └── korgau_upd.csv             
├── Dockerfile                     # Имидж для веб-сервиса Streamlit
├── docker-compose.yml             # Поднятие связки Postgres + Streamlit локально
├── notebooks                      # Разведочный анализ данных (EDA)
├── pyproject.toml
├── README_EN.md
├── README.md
└── uv.lock          
```

## Установка и запуск (Docker)

Для корректной работы всего стэка рекомендуется использовать Docker и Docker Compose.
_Для использования умного AI-чата у вас должен быть запущен локальный сервер [Ollama](https://ollama.com/) с моделью `llama3` (`ollama run llama3`), к которому Docker будет обращаться через порт 11434._

1. Запустите СУБД PostgreSQL в фоне:
```bash
docker-compose up -d db
```

2. Выполните скрипт первичной миграции данных (загрузка CSV-файлов в таблицы PostgreSQL):
```bash
uv run python -m app.utils.init_db
```

3. Запустите web-интерфейс:
```bash
docker-compose up -d web
```

4. Откройте панель управления в браузере:
[http://localhost:8501](http://localhost:8501)
